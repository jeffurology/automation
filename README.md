# automation

Automation repository.

## MCP servers

This repo configures a [Playwright MCP](https://github.com/microsoft/playwright-mcp)
server via `.mcp.json` at the repo root, so browser-automation tooling is
available automatically in Claude Code sessions (local and web).
